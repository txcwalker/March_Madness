# WORKLOG.md

Reverse-chronological session ledger. Newest entry on top. Each entry: date, what was done, what's next. Keep entries factual and brief — this is a ledger, not a narrative.

---

## 2026-08-03

Opened by walking the user through exactly how Path of Least Resistance's numbers are calculated (they'd lost track of the mechanics over the gap since 2026-07-31), with a worked arithmetic example using Michigan's real row, then answered a follow-up on what "Round of 32" as a column header actually means (Kaggle's slot-naming convention names a slot for the round its *winner enters*, not the round the game is played in -- "Round of 32" is a team's very first game, not a game played while already a Round-of-32 team; confirmed by re-reading `bracket/structure.py`'s `ROUND_NAMES`). No code changes from this -- purely a comprehension pass, confirmed with the user before moving on. Also showed the roadmap in its original clean milestone-list form (the working files have grown very narrative/verbose with accumulated session detail since 2026-07-22).

**Seed Prediction (Milestone 3) retired, nothing built.** The user reconsidered the original motivation and found real structural holes, not just execution gaps: seed is a noisy committee-process label (NET/Quad-1-4 criteria plus bracket-balancing mechanics like S-curve rules and avoiding early rematches, not pure team strength), citing a real 2024 example -- Memphis got a 5-seed while playing like a 9-10 seed, and the *existing* win-probability model already caught that (56% Colorado State) with zero seed-prediction infrastructure, since it compares real KenPom stats directly and never needed either team's seed. Walked through both of the milestone's two motivating use cases and found each better served without predicting a seed at all: region strength via predicted-seed clustering would have been a lossy round-trip through an artificial 1-16 bucket when real strength is already directly measurable; flagging mis-seeded teams doesn't need a seed-prediction model, only a direct comparison of the win-probability model's real output against what the seed gap alone implies -- almost word for word already Milestone 4's stated purpose, so that one idea moved there instead of becoming new infrastructure. `README.md`/`GOAL_TRACKER.md`/`AGENTS.md` updated: Milestone 3 marked retired (not renumbered, to avoid churning every existing cross-reference to Milestone 5/6/7 by number), Milestone 4 notes it absorbed the mis-seed-flagging idea, and three now-orphaned "worth testing in Milestone 3" notes (a program-identity feature, and a model calibration follow-up -- neither of which was actually about seed prediction, just filed under the next open milestone number at the time) were updated to read as unscheduled rather than pointing at a retired milestone.

**Region Strength revisited** -- the user's real complaint: the existing top-seed-share metrics measure whether the *presumed favorite succeeds*, not whether the region's own games are actually *close*. Concrete scenario raised: a region could look "fragile" by the existing metric if a strong team keeps winning it, while every one of its games is secretly a coin flip -- or look "dominant" while the favorite's games are all close and it just keeps winning anyway. Split this into two problems and designed a metric for each before writing code, per the user's request to see a full worked example first:

- **Problem 1 (mathematically involved, needed a full walkthrough):** `region_competitiveness()` -- averages outcome uncertainty (`2 × (1 - favorite's win probability)`, 1.0 = coin flip, 0.0 = a lock) across every game actually played within a region (the real Kaggle slot codes were checked against the live 2026 data to confirm: `R1W1`..`R4W1` are a region's own 15 games, `R5WX`/`R5YZ`/`R6CH` pair two different regions and are correctly excluded), across all 10,000 simulated brackets. Walked a 4-team, 2-round toy region by hand (12 individual game-closeness values averaging to 0.625) so the mechanism was fully transparent before it got built for real. User caught something important and correct along the way: this measures *unpredictability of the winner*, not margin of victory -- a 90/10 favorite can still win by one point. Confirmed this is fine for a region-strength stat (outcome uncertainty is literally the right thing to measure here) but flagged it as a naming/copy concern for the actual page, not a math problem.
- **Problem 2 (visually obvious, needed a number):** `region_effective_contenders()` -- 1 / HHI (Herfindahl-Hirschman Index, the standard economics tool for market concentration), computed on `benefit_if_team_loses()`'s existing same-region conditional Final Four shares (TeamX = the region's favorite). Worked the real Duke-region numbers from earlier in the week by hand (Connecticut 52.7%, Michigan St 15.1%, St John's 14.4%, Kansas 11.3%, ...) to HHI≈0.335, effective contenders≈3.0. User was surprised this didn't skew closer to 1 or 4 given Connecticut looked like "a huge favorite" -- checked the textbook case (a clean 50% favorite + three exactly-equal 16.7% backups) and got exactly 3.0 too, confirming the real messy data was behaving correctly, not underselling Connecticut's lead.

Built both for real. `analysis/region_strength.py`: `region_of_main_bracket_slot()` (region letter from an R1-R4 slot code), `region_competitiveness()`, `strongest_team_by_region()` (picks each region's favorite by `average_win_probability()`, deliberately not by seed -- same reasoning as the Seed Prediction retirement above), `region_effective_contenders()`. `region_competitiveness()`/`strongest_team_by_region()` need the win-probability matrix, so they're computed in `run_pipeline.py` (same reason `path_of_least_resistance` lives there) and written to a new `region_competitiveness.csv`; `region_effective_contenders()` only needs `benefit_if_team_loses()`'s output plus that CSV's favorite-team column, so it's computed in `export_site_data.py` off a `benefit` DataFrame now built once and shared between the region payload and the Who Benefits payload (previously built twice, redundantly). New `tests/test_region_strength.py` (5 tests, a 2-region/8-team fixture with a real upset in one region and none in the other, to exercise both the "fixed every bracket" and "depends on who actually won" cases the same way `test_bracket_path.py` does) plus a textbook-case test reproducing the 3.0 hand-check.

Full suite: 96 passing (was 91). Ran the real pipeline end-to-end (31s, no meaningful slowdown from the new ~600K-iteration region walk) and `export_site_data.py`, verified in a fresh dev server (the same stale-long-running-dev-server issue from 2026-07-31 recurred -- restarted it, not a code bug). Real numbers told a coherent, differentiated story across all four regions: Region W (Duke) scores lowest on both outcome uncertainty (37.2%) and effective contenders (3.0, matching the hand-check) -- the most predictable region either way. Region X's model-favorite is **Houston, not Florida the #1 seed** -- a concrete validation that `strongest_team_by_region()` is doing its job, not just a theoretical concern. Region Y (Michigan) has a *high* top-seed Final Four share (68.2%) but the *most* effective contenders (4.9) if Michigan falls -- exactly the "dominant favorite, wide-open backup field" case that motivated building this in the first place. Added a `RegionStrength.jsx` section ("Outcome Uncertainty & Effective Contenders") alongside the existing top-seed-share table, with copy that explicitly says this measures winner-unpredictability, not score margin. Docs updated (`README.md`, `AGENTS.md` including a new Fragile Areas note on the outcome-uncertainty/margin-of-victory distinction, `GOAL_TRACKER.md`).

**Next:** confirm with user before committing/pushing (Seed Prediction retirement + Region Strength extension, all uncommitted). After that: Milestone 4 (Upset Finder & Cinderella Stories, now also home to mis-seed flagging) is the natural next real feature; Post-Mortem and Previous-Years History are still the two not-started Milestone 5 items.

---

## 2026-07-31

Picked up exactly where 2026-07-30 left off: the user's open feedback that Path of Least Resistance "needs some work still," now with specifics. The concrete complaint: raw PathEase (the team's own win-probability product against the toughest possible opponents) mechanically favors strong teams regardless of draw difficulty — a 1-seed's win probability is high against almost anyone, so it will always look like it has the "easiest" path even when its actual opponents (the best available 2, 3, 5, 8, 9 seeds a bracket routes into one section) are genuinely tough. Both prior versions (most-likely opponent, then toughest-possible-opponent) had this same flaw, since neither normalized for the team's own strength.

Talked through the fix before writing code, per the user's request. Considered a ratio/residual normalization (PathEase divided by a baseline like the team's own strength^4) but rejected it: later rounds structurally face stronger opponents than a random field team even in the "normal" case, so the ratio would mechanically read as "harder than expected" for every good team late in the bracket, hiding the real signal. Landed instead on two separate, never-collapsed metrics: **PathStrengthFaced** (average of the *opponents'* own field-wide strength score, `average_win_probability()`, never referencing the team's own win probability at all — seed-agnostic by construction) alongside **PathEase** (kept as the team-relative "given this draw, am I still favored" number).

Combined this with the other open thread from 2026-07-30's session end (not yet acted on): accounting for *every* possible matchup at each round, not just a single best- or worst-case guess. Rather than inventing new subtree-weighting logic, reused the raw per-bracket Monte Carlo results (`run_monte_carlo()`'s long-format `Bracket, Slot, TeamID` output, already sitting in scope in `run_pipeline.py` right before `path_of_least_resistance()` is called, previously discarded after being written to `simulation_results.csv`). Rewrote `bracket_path.py`: new `resolve_slot_winners_by_bracket()` groups that output into one `{slot: winner}` lookup per simulated bracket; `path_of_least_resistance()` now walks each team's own chalk-advancement chain across all 10,000 brackets and, at each round, looks up whoever *actually* won the opposing slot in that specific bracket — averaging PathStrengthFaced across all (round, bracket) samples, and averaging PathEase as the mean of each bracket's own win-probability *product* (not the product of means — preserves the correct expected value and the within-bracket correlation of a single realized bracket path). `build_subtree_teams()` (the old worst-case-picking helper) was removed as no longer needed.

Wired `results` into the `run_pipeline.py` call site, rewrote `export_site_data.py`'s payload builder (per-round opponent-name fields replaced with per-round strength-faced numbers; sorts ascending by `pathStrengthFaced`), and rebuilt the frontend page: opponent-name columns became round-by-round strength percentages, with Path Strength Faced (primary, default sort) and Path Ease (secondary) as the two headline columns. Rewrote `tests/test_bracket_path.py` around a 3-bracket fixture that deliberately varies one round's outcome (a team's Sweet-16 opponent is team 13 in two brackets, team 14 in the third) to verify the new averaging behavior directly, plus a new `resolve_slot_winners_by_bracket()` unit test.

Full suite: 91 passing (was 90). Re-ran the real pipeline end-to-end (`run_pipeline.py`, 31s for the full run including 10,000 brackets and the new per-team Monte Carlo path walk — no meaningful performance regression) and `export_site_data.py`, then verified in a fresh dev server: Michigan now has the easiest draw (lowest PathStrengthFaced, 50.2%), and Duke — the title favorite — sits 5th, distinctly tougher than Michigan/Florida/Houston/Arizona (53.9% vs. 50.2-53.3%) despite still carrying a high PathEase (64.1%, still favored). Exactly the "team is good but the draw isn't easy" distinction the old single-number metric couldn't show. Docs updated (`README.md`, `AGENTS.md` including a new Fragile Areas note on why PathEase alone shouldn't be used as a draw-difficulty ranking, `GOAL_TRACKER.md`).

Also answered a standing housekeeping question this session: confirmed `README.md`/`AGENTS.md`/`GOAL_TRACKER.md`/`WORKLOG.md` were current as of 2026-07-30's session end (uncommitted, but not stale — this session picked up exactly where that entry's "Next" line left off).

User reviewed the rebuilt Path of Least Resistance page and flagged that the top 5 easiest-draw teams by seed were 1, 1, 1, 2, 1 — still mostly 1-seeds, even with PathStrengthFaced no longer referencing a team's own win probability. Not treated as a re-open: the actual real-data numbers (Michigan 50.2%, Florida 50.6%, Houston 52.1%, Arizona 53.3%, Duke 53.9%) show real separation and Duke (title favorite) sitting behind three other top-10 teams despite being the strongest team in the field by win probability — the metric is doing its job, it's just that this year's top 3-4 teams happen to be unusually far ahead of the rest of the field on both strength AND, this year specifically, draw. User's own read: this metric is probably most informative in the 4-7 seed range, and wants to think about it further before deciding on any more changes. No code change made; left as-is per explicit instruction.

User then flagged a real, separate bug in Who Benefits, caught by testing with Akron (a massive underdog) as TeamX: Michigan's Final Four odds showed "0.0% → 68.2%," which reads as "Akron is always in the Final Four" — not the intended meaning, and not true. Root cause: the "before" value was `FinalFourShareIfXAdvances`/`ChampionShareIfXAdvances` — TeamY's odds conditioned on TeamX *reaching* the Final Four. For a team like Akron that essentially never reaches the Final Four in any of the 10,000 simulated brackets, that conditioning subset is tiny-to-empty, so every TeamY's "before" collapsed to ~0% regardless of TeamY's real odds — the intended baseline was TeamY's ordinary, pretournament odds, not a second conditioned subset.

Fixed in `benefit_if_team_loses()`: replaced the "if TeamX advances" conditioned value with TeamY's true unconditional (full-sample) rate as the baseline for every pair, same-region or not — this was already being computed as `overall_champion_share`/`overall_final_four_share` for the cross-region case, just not reused for same-region pairs. Kept the "if TeamX eliminated" conditioned value (the real "TeamX loses" case) as the "after." This also removed the now-unused "if TeamX advances" bracket-subset computation entirely, simplifying the function. Renamed the CSV/JSON fields (`ChampionShareIfXAdvances` → `ChampionShareBaseline`, same for Final Four) through `export_site_data.py` and `WhoBenefits.jsx`; updated the page copy and section notes to describe "baseline vs. TeamX eliminated" instead of "in vs. out." Updated `tests/test_analysis.py`'s `test_benefit_if_team_loses_flags_who_gains_when_a_team_is_eliminated` for the new baseline values (worked through the synthetic 2-bracket fixture by hand: same-region benefit changed from 1.0 to 0.5 now that baseline is 0.5 instead of 0.0, cross-region pair still forces baseline == conditioned == 0.5 with Benefit exactly 0).

Full suite: 91 passing. Re-ran `export_site_data.py` (no need to re-run the full pipeline — this analysis only reads `simulation_results.csv`) and re-verified in the browser: picking Akron now correctly shows every team's bar barely moving at all (correct — Akron was already an almost-certain early exit, so conditioning on that removes almost nothing from the full sample); picking a real contender like Kansas shows a small, plausible, non-dramatic shift (Duke's Final Four odds 64.7% → 67.4%, championship 41.0% → 42.7% if Kansas is eliminated). Docs updated (`README.md`, `AGENTS.md` including a new Fragile Areas note, `GOAL_TRACKER.md`).

User reviewed the fixed page and caught a real, separate over-correction from 2026-07-30's session: championship odds were still region-scoped (forced to baseline for a different-region TeamY, same as Final Four odds), but the user pointed out this is wrong for championship odds specifically -- if Duke loses, it should affect *everyone's* title odds, not just Duke's own region. Correct: these are genuinely different questions. A different-region team's odds of *reaching* the championship game are exactly independent of Duke (proven -- regions resolve independently until the national semifinals, so Final Four region-scoping is correct and stays). But a different-region team's odds of *winning* the championship game depend on who they'd face there, which comes from the other half of the bracket -- eliminating a strong team makes the eventual final easier for literally everyone, any region, a real structural effect the 2026-07-30 fix had incorrectly zeroed out as "indistinguishable from noise."

Fixed in `benefit_if_team_loses()`: `ChampionShareIfXEliminated` now always uses the real TeamX-eliminated bracket subset for every TeamY, same-region or not (removed the region check for this one column only); `FinalFourShareIfXEliminated` keeps the region-forcing, since that one really is mathematically exactly zero for a different region. Updated the docstring to explain why the two metrics get different treatment (a "why these differ" note, not just "what changed"). Updated `tests/test_analysis.py`'s cross-region assertions: the (101, 202) pair's `ChampionShareIfXEliminated` is now the real subset value (1.0, matching the same-region row) instead of being forced to the baseline (0.5), while `FinalFourShareIfXEliminated` still equals the baseline.

Full suite: 91 passing. Re-ran `export_site_data.py` and re-verified in the browser with Duke as TeamX: Michigan and Arizona (both cross-region -- their own Final Four odds correctly stay exactly flat at 68.2%/58.2%, confirming the region check is still working for that metric) now show a real, substantial championship-odds gain if Duke is eliminated (17.2%→29.6% and 21.1%→29.6%), alongside same-region Connecticut's larger combined move (which also gets a Final Four boost, 18.6%→52.7%). Docs updated (`README.md`, `AGENTS.md` including a Fragile Areas note explaining why this isn't an inconsistency, `GOAL_TRACKER.md`).

User asked explicitly for the two chart sections to reference independently-ranked data: the Final Four table should always be region-specific, the Championship table should never be. They'd previously shared one server-sorted top-10 (by championship benefit), so the Final Four section could show cross-region teams with zero real movement instead of TeamX's actual regional rivals. Fixed entirely on the frontend, since `export_site_data.py` already sends every team's full row uncapped: `WhoBenefits.jsx` now independently sorts and slices each section with its own `useMemo` -- Final Four by `finalFourBenefit` (exactly 0 outside TeamX's region per the region-scoping fix above, so this ranking surfaces the region on its own with no explicit filter needed) and Championship by `championBenefit` (real for any team, any region). Updated the lede copy to explain the distinction and each section's note line to say which scope it's showing.

Hit an unrelated snag verifying this in the browser: clicking into Who Benefits after the JSX edit threw "An error occurred in the <WhoBenefits> component" and silently fell back to rendering Home, on a dev server that had been running continuously since early in this session through many HMR updates -- the same stale-HMR failure mode logged on 2026-07-30 ("long-running Vite process... stopped picking up new pages correctly"). Confirmed it wasn't a real code bug: killed the stale `npm run dev`/vite processes, started fresh, and the page rendered correctly on the first load, including via direct `#who-benefits` hash navigation and normal click navigation. Worth reiterating as the standard fix if this recurs -- restart the dev server before assuming a fresh code change is broken.

Re-verified with a fresh server: Duke's Final Four table now shows only Duke's region (Connecticut, Michigan St, St John's, Kansas, Louisville, UCLA, UCF, Ohio St, TCU, South Florida); the Championship table mixes in cross-region teams (Michigan, Arizona, Houston, Florida, Gonzaga, Vanderbilt) alongside them. Akron re-checked too: Final Four table still correctly Akron's-region-only, and every value in both tables barely moves, as expected for a near-certain early exit. Docs updated (`README.md`, `GOAL_TRACKER.md`).

**Next:** confirm with user before committing/pushing (this repo is currently 2 commits ahead of `origin/main`, plus all of today's and yesterday's Path of Least Resistance / Who Benefits work still uncommitted). User is separately still thinking about whether Path of Least Resistance needs further work (no action item yet — revisit only if the user brings specifics). After that: Milestone 3 (seed prediction depth + the flagged model-calibration question) when the user has bandwidth for the clustering design work; Post-Mortem and Previous-Years History are the two Milestone 5 items still not started.

---

## 2026-07-30

Picked up the parked Milestone 2 presentation-style question from the prior session's ending, working from `../March_Madness_2026` where the visual comparison happened. Corrected a wrong assumption made mid-session: initially told the user to "keep Jinja2," having only skimmed `GOAL_TRACKER.md` and the leftover `site/` files, not `AGENTS.md`/`WORKLOG.md`'s fuller record — the Jinja2 build had already been shown to the user, rejected, and pivoted to Vite + React before this session started. Corrected once found.

Confirmed direction: **Minimal Editorial**, both light and dark themes designed with equal care (not light inverted into dark). Moved `content/`, `site/`, `docs/`, `scripts/build_site.py` to `legacy/jinja_site/` (never committed, so no git history to clean up); dropped `jinja2`/`markdown` from `pyproject.toml`. Restructured the roadmap: inserted **Milestone 5 — Bracket Path & Historical Tools** (Path of Least Resistance, Who Benefits if Team X Loses, Post-Mortem/Year Retrospective, Previous-Years History) between the old 4 and 5, renumbering Sportsbook Integration and In-Season Modeling to 6/7.

Built Milestone 2 for real: `scripts/export_site_data.py` (reruns cached `simulation_results.csv` through `analysis/` and writes JSON, mirroring the role the superseded `build_site.py` played), `frontend/` (Vite + React, port 5181 — registered in `../LOCALHOST_PORT_REGISTRY.md` — mirroring `NFL_Exploration/frontend`'s router-free `pagesConfig.js` pattern). Six of seven analytics pages built and verified against real data in the running dev server: Home, Round Odds, Over/Underperformers, Cinderella Watch, Final Four Finder, Region Strength. Seed Prediction deliberately left as a placeholder — the user has learned more about clustering since the original design and wants to redo it before the 2027 publish, not ship the old approach.

User feedback after seeing the first pass drove three real fixes, not just polish:
1. **Region Strength was measuring the wrong thing.** The original top-heaviness stat conditioned on the region winning the whole national title, which conflated a region's internal competitiveness with cross-region championship-game strength and was statistically unstable for any region that rarely won it all (one region's figure came from ~120 of 10,000 brackets). Added `region_top_seed_final_four_share()` (unconditional, all 10,000 brackets) as the primary "fragile → competitive" metric; kept the old conditional stat as a secondary figure rather than deleting it.
2. **A silent data-integrity bug**, surfaced by switching the model to xgboost_model: a team that never wins a single simulated game (Furman, Prairie View, under a more confident model) was silently dropped from the entire site export instead of showing at ~0% — the same class of bug `average_wins_by_team()` already guards against. Fixed by anchoring `export_site_data.py`'s team payload on the full `teams.csv` roster.
3. **Final Four Finder rebuilt** around four region dropdowns (Any + every team, sorted by seed) rather than a text filter, per the user's actual spec — bar chart + list of matching combos, defaulting to top 10. `export_site_data.py` now ships the full ~266-combo list instead of a capped top-40, so an arbitrary partial selection is answered honestly, including "never occurred."

User pushed back hard on Duke's 62.6% title share under xgboost_model as implausible. Built `scripts/evaluate_models.py` (trains all four candidate models on an identical held-out split, prints a metrics table including expected calibration error — which `models/common.py` already computed but `run_pipeline.py` never printed — and plots a real reliability diagram to `reports/model_calibration.png`). Confirmed the user's instinct: xgboost_model was worse than logistic_regression on *every* metric (accuracy 0.681 vs 0.745, ECE 0.145 vs 0.032), not just calibration. Reverted the default back to `logistic_regression` immediately.

Didn't stop at "xgboost needs tuning," though — user asked directly what changed between this rebuild and the legacy project's models, since only logistic regression (and, on closer look, random forest) held up; XGBoost and the neural net specifically cratered. Investigated rather than guessed: the real cause was training-data volume. `data/raw/` had only one populated season (2026, 5,265 games) against the legacy project's 23 seasons (`_allgames2.csv`, ~120,000 games) — a ~23x reduction that hits high-variance learners far harder than a regularized linear model or a bagged ensemble.

Fixed with a real backfill, not a workaround. `scripts/backfill_historical_kenpom.py` imports the legacy project's already-merged `data/kenpom_fin_df.csv` (2003-2025; 2026 deliberately excluded in favor of this repo's own fresher, independently-verified raw pull) into `data/processed/<year>/kenpom_clean.csv`. Extended `ingest/kenpom.py`'s `build_kenpom_history()` to also read those for any year without a raw export of its own (raw still wins if both exist), with new tests. This surfaced a second, previously-unknown variant of the Excel W-L date-mangling bug along the way: the existing fix only recovered a mangled *loss* count (`"20-12"` → `"20-Dec"`); real 2010/2012 data had mangled *win* counts too (`"2-29"` → `"Feb-29"`), silently producing `NaN` and crashing model training three layers deep in sklearn before it was traced back. Fixed by renaming `parse_losses` to `parse_win_loss_component` and applying it to both sides of the split, not just one, with a new test.

Post-backfill, on 24 seasons instead of 1: xgboost_model accuracy 0.681→0.744, ECE 0.145→0.024; neural_net accuracy 0.701→0.753, ECE 0.077→**0.007** (now the best-calibrated of the four). Logistic regression stayed marginally best on every metric, so the live default hasn't changed — but XGBoost and the neural net are now legitimate contenders for a real Milestone 3 comparison, not disqualified by a data problem unrelated to the algorithm.

User discussed, at length, why they're now less opposed to a team/program-identity feature than they'd have been five years ago: college basketball's shift from the one-and-done era (best recruits scattered across blue-bloods for a single year each, pre-2021ish) to the NIL/transfer-portal era (players climb from smaller programs to whichever school can pay the most, concentrating sustained talent-acquisition advantage at wealthy programs specifically). Agreed TeamID stays out of the feature set for now — legacy's inclusion of raw `TeamID_A`/`TeamID_B` was also a real identity-leakage bug (row-random split let a team's ID leak across train/test, inflating legacy's old RF/XGBoost/NN numbers) — but a non-leaky program-identity or program-tier feature is worth deliberately testing in Milestone 3, not assumed away either direction.

User confirmed and asked to commit, switch the live default to `xgboost_model` anyway (judging 0.744 vs. logistic_regression's 0.755 close enough to prefer XGBoost's nonlinear capacity), and see the metrics table. Committed (`2fd252c`, not pushed). Re-ran the pipeline with `xgboost_model` — Duke came back at 67.5%, *higher* than the number originally flagged as implausible, despite everything above being fixed.

User asked directly to check for leaks rather than accept it. Investigated rather than re-running the same calibration check: tested whether the fitted model's predictions are symmetric to which team's stats land in the "_A" feature slot vs. "_B" for the exact same real matchup. They're not, and the gap is large — Arizona vs. Michigan (nearly identical KenPom ratings) came back 64%/38% one way, ~38%/62% reversed; Duke vs. Arizona summed to ~165% instead of ~100% across the two directions. Root cause: `bracket/simulate.py`'s `compute_win_probabilities()` used the raw, un-symmetrized prediction directly, and `simulate_bracket()` always resolves a slot's StrongSeed as "_A" — so whichever team's bracket position is structurally labeled StrongSeed got a compounding, unearned edge every single round, regardless of real team strength. `randomize_matchup_sides()` (training-time) doesn't prevent this; it only stops the model learning a systematic ordering pattern from historical data, it doesn't guarantee the fitted model is symmetric for any specific matchup — a real gap the earlier calibration check couldn't have caught (that check's own test set was already side-randomized per row, which decorrelates slot position from calibration bins by construction).

Fixed by symmetrizing: average the forward prediction against the complementary probability implied by the reverse-slot prediction, so `probs[a][b] + probs[b][a] == 1.0` always holds. New test (`test_compute_win_probabilities_symmetrizes_away_a_slot_position_bias`) uses a deliberately slot-biased fake model to confirm the fix. Re-ran the pipeline: Duke's title share dropped 67.5%→**41.0%**, with Arizona (21.1%) and Michigan (17.2%) both coming up to numbers that actually make sense given how close their ratings are. This was a real simulation-methodology bug, not a modeling or data problem — likely affected every model's simulated (not per-game) output to some degree, just most visibly for XGBoost given its higher capacity to learn slot-position artifacts.

Full test suite: 84 passing (was 79 at the start of this session, 83 before this fix).

User confirmed the symmetrization fix and committed it (`04b2ffd`). Separately noted Duke still looks a little high at 41.0% (expected ~30% for a tournament favorite) — logistic_regression's 32.2% is closer to that intuition. Not a bug (slot-symmetry already verified); a real model-to-model disagreement in how sharply XGBoost separates strong teams, amplified by 6 rounds of compounding. Logged as a Milestone 3 calibration item rather than hand-adjusted now.

Asked to be reminded of Milestone 3's scope (Seed Prediction depth, per the roadmap) before heading out, then asked for something more hands-off to make progress on while away, since Milestone 3 needs the user's own clustering judgment. Picked two Milestone 5 items that were already well-specified from an earlier conversation and don't need further design input: Path of Least Resistance and Who Benefits if Team X Loses. Built both with the same rigor as the rest of this session (real analysis functions, tests, JSON export wiring, frontend pages, verified in the browser):

- **Path of Least Resistance** — new `analysis/bracket_path.py`. `build_feeds_into()` maps the bracket's slot tree; `path_of_least_resistance()` walks each team's own seed forward through the four rounds before the Final Four to find its expected opponents (assuming everyone else also advances by chalk), scoring "PathEase" as the product of the team's own win probability against each. Needs the win-probability matrix and a full chalk-bracket resolution (`bracket.simulate.simulate_bracket(..., deterministic=True)`), both only available inside `run_pipeline.py`'s own run -- computed there and written to `path_of_least_resistance.csv` for `export_site_data.py` to read, the one export field not rebuilt purely from `simulation_results.csv`. Verified against real 2026 data: Michigan has the softest draw (72.5% path ease, through UMBC/Georgia/Alabama/Virginia); Duke's is tougher (64.3%, hits Connecticut in the Elite Eight) -- a sensible, independently interesting result distinct from raw Final Four probability.
- **Who Benefits if Team X Loses** — new `benefit_if_team_loses()` in `analysis/round_advancement.py`. Operationalized "loses" as "doesn't reach the Final Four" (round R4); computed entirely from the existing `simulation_results.csv` by comparing each other team's championship share conditioned on team X reaching vs. not reaching the Final Four, no new simulation needed. Frontend page has a team-select dropdown and a diverging bar chart (reused the Over/Underperformers pattern). Verified: if Duke doesn't reach the Final Four, Michigan gains the most (+19.2 points), then Arizona (+13.1), then Connecticut (+11.7, up from a near-zero baseline) -- intuitive, since Connecticut sits in Duke's own bracket path.

Both required restarting the dev server mid-verification -- a long-running Vite process (alive since much earlier in the session, through dozens of HMR updates) stopped picking up new pages correctly; a fresh `npm run dev` fixed it immediately. Not a code bug, just stale dev-server/HMR state after an unusually long session -- worth remembering as a troubleshooting step, not a red flag on the new code, if this recurs.

Full test suite: 88 passing (was 84). Docs updated (`GOAL_TRACKER.md`, `README.md`, `AGENTS.md`) including two stale-reference corrections caught along the way (`AGENTS.md` still said the default model was `logistic_regression` and still listed old six-page frontend file list in two separate places, both leftover from edits earlier in this same session that didn't get fully propagated).

User reviewed both tools and asked for two real refinements, not just polish:

1. **Path of Least Resistance was answering a slightly different question than intended.** User wanted each round's opponent to be "the best team they could [face]," not the model's most-likely winner of that branch -- a worst-case gauntlet, not an expected-case one. Revised `bracket_path.py`: added `build_subtree_teams()` (every team that could possibly occupy a slot, accumulated bottom-up) and `average_win_probability()` (a simple, self-contained strength score), and the path walk now picks the strongest team in the whole opposing subtree at each hop -- no longer needs a chalk-bracket resolution at all, which simplified `run_pipeline.py`. Real effect: Michigan's Elite Eight opponent changed from Alabama (chalk) to Texas Tech (the actual toughest team lurking in that bracket branch); Duke's from St John's to Kansas. Also asked directly whether the aggregation should be a product or a sum of the four round probabilities -- recommended keeping the product (already what was built): these are sequential elimination games that compound, so a sum would hide a single dangerous team by averaging it against three easy ones (concretely, 0.9^4=66% vs. 0.99^3×0.5=48% are very different real risk profiles that a sum would score almost identically).
2. **Who Benefits needed Final Four odds, not just championship odds, and absolute values, not just the delta.** Extended `benefit_if_team_loses()` to also return Final-Four-conditioned shares -- a same-region rival can gain enormously at the Final Four level (Connecticut 0%→52.7% if Duke doesn't make it) while a different-region team gains much less there but still meaningfully at the championship level (Michigan, Arizona) -- one metric alone hides that distinction. Rebuilt the frontend around stacked "before → after" bars (e.g. "10.4% → 29.6%") instead of a bare "+19.2%" delta, in two sections (Final Four, Championship) sharing one team-select dropdown.

Both re-verified against real data with a fresh dev server. Full test suite: 90 passing (was 88; `test_bracket_path.py` rewritten for the new subtree-based logic, `test_analysis.py` extended for the new Final Four fields).

User caught a real correctness bug in Who Benefits on the next look: teams from a *different* region than TeamX were showing small nonzero Final Four "benefit" (e.g. Michigan +0.6 points if Duke doesn't reach the Final Four) -- "that should not be happening at all." Verified rather than assumed: computed the expected standard error for splitting ~10,000 brackets into a ~6,500/~3,500 subset at p≈0.68 (≈0.98 points) and found it matched the observed shifts almost exactly, confirming pure Monte Carlo sampling noise, not a formula bug. The real fix: each region resolves its own Final Four representative entirely independently of every other region (no shared game, no shared random draw, until the national semifinals), so a different-region team's Final Four odds are *mathematically guaranteed* unaffected by TeamX -- any nonzero movement there is definitionally noise. `benefit_if_team_loses()` now takes a `team_to_region` mapping; for cross-region pairs, both conditioned columns report TeamY's unconditional (full-sample) rate, forcing Benefit to exactly 0 rather than exposing sampling noise as a false signal. Same-region pairs (a real, structural effect -- only one team per region reaches the Final Four) are untouched. Re-verified: Duke's ranked list now correctly shows only Duke's own region (Connecticut, Kansas, Michigan St, St John's, etc.) as beneficiaries; every other region shows exactly 0%. Added a Fragile Areas note in AGENTS.md generalizing the lesson for future conditional-probability analyses on the Monte Carlo results.

User also said Path of Least Resistance "is ok but I think it needs some work still" without specifics -- asked for what specifically feels off rather than guessing at more changes, since the last revision already changed the core methodology once based on stated intent.

Full test suite: 90 passing (test fixtures updated for the region-scoped conditioning, including a same-region case with a real effect and a cross-region case asserting exactly-zero benefit).

**Next:** get specifics from the user on what still feels off about Path of Least Resistance. Confirm with the user before committing/pushing this round (Path of Least Resistance + Who Benefits, including all of today's refinements — not committed yet). After that: Milestone 3 (seed prediction depth + the flagged model-calibration question) when the user's back with bandwidth for the clustering design work; Post-Mortem and Previous-Years History are the two Milestone 5 items still not started.

---

## 2026-07-22

Initialized the project. Audited the prior codebase (`../March_Madness_2026`, née `txcwalker/March_Madness_2024` on GitHub) and found it functionally broken: a missing/broken import chain, hardcoded season year and bracket size scattered across files, an undocumented manual Excel step for cleaning KenPom exports, and duplicate `.py`/`.ipynb` implementations that had drifted out of sync. Decided against a full logic rewrite — the modeling/domain logic (KenPom cleanup rules, bracket slot mechanics, conference tiers) is sound and expensive to rederive; only the structure/plumbing needs rebuilding. Agreed on a target repo structure (config-driven season/bracket settings, `src/` as sole production code, notebooks exploration-only, automated KenPom ingest). Created this repo at `March_Madness/`, initialized git, and scaffolded the documentation backbone (`README.md`, `AGENTS.md`, `DEVELOPMENT.md`, `WORKLOG.md`, `GOAL_TRACKER.md`).

**Next:** repo skeleton (`config/`, `src/march_madness/`, `scripts/`, `notebooks/`, `tests/`, `data/`, `reports/`), dependency manifest, and `.gitignore`.

Before committing, defined the full project roadmap with the user. Established one standing priority (audit/cleanup/reorganization/modularity — ongoing, never "done") plus six sequential milestones: (1) port & modularize the existing product at feature parity [current focus], (2) presentation/visualization, (3) seed prediction depth, (4) upset finder & Cinderella stories, (5) sportsbook/prediction market API integration, (6) in-season predictive modeling incl. bubble-team at-large prediction — explicitly a ~2-year-out stretch goal since preseason data alone is weak for this. Updated `README.md` and `GOAL_TRACKER.md` to reflect this structure.

**Next:** review roadmap with user, then commit the documentation backbone and start on Milestone 1 (repo skeleton).

Committed and pushed the documentation backbone to a new public GitHub repo (`txcwalker/March_Madness`) after confirming visibility/name with the user. User asked that pushes to any remote/live repo always get a check-in first, even when the surrounding task was already approved — saved as a standing cross-project preference.

Built the Milestone 1 repo skeleton: `config/`, `src/march_madness/{ingest,features,models,bracket,analysis}` (empty `__init__.py` placeholders), `scripts/`, `notebooks/`, `tests/`, `data/{raw,processed,outputs}`, `reports/`. Added `pyproject.toml` (src-layout, dependencies carried over from the legacy `.venv` plus `pyyaml`) and extended `.gitignore` for packaging artifacts. Nothing has real logic yet — next steps are `config/season.yaml` + loader, then the bracket structure module.

**Next:** confirm with user before committing/pushing the skeleton, then start on `config/season.yaml` schema + `src/march_madness/config.py` loader.

User confirmed the skeleton matches the agreed architecture. Two new requirements surfaced, both elevated to standing priorities in `README.md`/`GOAL_TRACKER.md` alongside the audit/cleanup one: (1) year-over-year reuse is a hard requirement, not just a nice-to-have — `data/{raw,processed,outputs}` need a per-year layout so seasons coexist and `config/season.yaml`'s `year` field is a real toggle, decided alongside the upcoming config module; (2) post-mortem/performance evaluation tooling (comparing predictions to actual results after a tournament) is wanted but explicitly deferred — user wants to co-design it once there's a real season of predictions to evaluate against, not spec it now.

**Next:** build `config/season.yaml` schema + `src/march_madness/config.py` loader, including the per-year data directory convention.

Built and verified the config module: `config/season.yaml` (year + bracket size/rounds/play-in count), `src/march_madness/config.py` (`load_season_config()` returns a validated `SeasonConfig` with `raw_dir`/`processed_dir`/`outputs_dir` derived per-year under `data/<kind>/<year>/`; `ensure_data_dirs()` creates `processed`/`outputs` but deliberately leaves `raw` alone so a missing input dir fails loudly rather than silently). Added `tests/test_config.py` (7 tests) and a `dev` extra (`pytest`) to `pyproject.toml`. Discovered the system default Python was 3.8 — recreated `.venv` against the Python 3.10 install the legacy project used, upgraded pip (old pip couldn't do editable installs from `pyproject.toml` alone), installed the package, ran the full test suite (7/7 pass), and sanity-checked `load_season_config()` against the real `config/season.yaml` with no arguments to confirm the default path resolves correctly. Updated `README.md`, `AGENTS.md`, `DEVELOPMENT.md`, and `GOAL_TRACKER.md` to match.

**Next:** confirm with user before committing/pushing, then start on `src/march_madness/bracket/structure.py` (format-agnostic rounds/slots).

User corrected a wrong assumption before this got built: `R1`...`R6` and the 64-team bracket are Kaggle's fixed data format, not something to abstract away — the sims are designed around it. What's actually variable is the play-in stage; user expects a future 76-team format to use 12 play-in games (vs. 4 today). Inspected the real `MNCAATourneySlots.csv`/`MNCAATourneySeeds.csv` from the legacy project to ground the design in the actual schema rather than assumptions, and derived `field_size = 64 + num_play_in_games` (68=64+4 today, 76=64+12 matches the user's expectation).

That inspection also surfaced a real bug: in the raw Kaggle file, play-in slots (e.g. `X16`) are listed *after* the R1 rows that reference them as a seed. The legacy simulator (`sims_mens.py`) iterates in raw file order with no sort, so it would look up an unresolved play-in winner — a genuine latent bug in the old code, not something to carry forward.

Built `src/march_madness/bracket/structure.py`: fixed R1-R6 round classification/naming (ported from the legacy `round_dict`), `validate_bracket_config()` enforcing the size/play-in-count invariant, `validate_slots()` for checking real season data against config, and `order_slots_for_simulation()` which fixes the play-in ordering bug. Added `tests/test_bracket_structure.py` (14 tests, covering both the current 4-play-in format and a synthetic 12-play-in/76-team scenario) and additionally verified the module directly against the real 2024 `MNCAATourneySlots.csv` from `../March_Madness_2026` — confirms the ordering bug is real and that the fix works on actual data, not just synthetic fixtures. Full suite: 27/27 passing.

Corrected `README.md`, `AGENTS.md`, and `GOAL_TRACKER.md` — they previously said (incorrectly, before this conversation) that hardcoding `R1`..`R6` would be a regression to avoid; fixed to reflect that those are legitimate fixed constants and `num_play_in_games` is the actual config-driven lever.

**Next:** confirm with user before committing/pushing, then start on `ingest/kaggle.py` and `ingest/kenpom.py`.

Built both ingest modules, grounded in real data the same way as `bracket/structure.py`.

`ingest/kaggle.py`: `load_kaggle_data(kaggle_dir)` loads the seven Kaggle CSVs the pipeline actually uses (teams, seeds, slots, regular season/tourney compact results, conferences, team conferences) into a `KaggleData` bundle, with a clear error if a file's missing. Verified against the real, full 2026 Kaggle data directory from the legacy project (198K+ regular season game rows, etc.) — loads cleanly.

`ingest/kenpom.py`: `clean_kenpom_export()` drops rank-subscript/blank columns, drops stray repeated header rows, splits Team/Seed and W/L apart, adds a Season column. `build_kenpom_history()` scans `data/raw/*/kenpom_raw.csv` and merges every available year — the automated replacement for re-pasting the full history by hand each season. Verified against the real `kenpom_2026_raw.csv` (365 teams) and found a real bug in the process: Excel silently reformats a `W-L` value like `20-12` into the date `20-Dec` whenever the loss count looks like a month number, corrupting ~90 of 365 teams' (25%) loss counts on every paste. Fixed by recovering the loss count from the month abbreviation. This had apparently been happening unnoticed. Deliberately did not reconcile KenPom team names to Kaggle `TeamID`s here (KenPom's names don't match Kaggle's `TeamID`-keyed names) — that belongs in `features/build_features.py`, next up.

40 tests total (was 27; +3 kaggle, +10 kenpom including the Excel bug case), all passing. Docs updated (README.md, AGENTS.md, GOAL_TRACKER.md).

**Next:** confirm with user before committing/pushing, then start on `features/build_features.py` (matchup pairing, conference tiers, and the KenPom-to-TeamID reconciliation deferred from ingest).

Built `features/build_features.py`, grounded in the legacy `ncaabwinsmodel.py`'s matchup-construction logic (read the real code rather than guessing). Two significant real-data findings, both fixed:

1. **Team-name matching**: an exact `Team` == `TeamName` match (what the legacy project did) matched only 228/365 real 2026 teams (62%) — KenPom's "Iowa St." never equals Kaggle's "Iowa State", etc. Kaggle ships `MTeamSpellings.csv` specifically for this; using it instead recovered 352/365 (96%). Added `team_spellings` to `ingest/kaggle.py`'s `KaggleData`/`_FILES` (this required going back and editing the already-committed ingest module). `match_kenpom_teams()` now returns both matched and unmatched rows, so the remaining ~4% stays visible instead of silently disappearing like before.

2. **Positional alignment bug**: the legacy tournament-game merge joined winner-rows to loser-rows by row position, not a real key — if either side's join with KenPom stats dropped a different row, this would silently pair unrelated games together. `build_matchup_history()` joins on an explicit `GameID` instead. Added a regression test that constructs exactly this scenario (one team missing KenPom data) and confirms the game is dropped cleanly rather than misaligned.

Also found, while verifying against real data: KenPom's conference abbreviations drift over time just like `AdjEM`→`NetRtg` did — the legacy `conf_mapping` used `Pat` for the Patriot League, but the real 2026 export uses `PL`, and was also missing `BW`/`SC`/`BSth` entirely. Without these, 10% of real matchup rows got a silently null conference; fixed by adding the missing/current abbreviations to `KENPOM_TO_KAGGLE_CONFERENCE`. Re-verified: 0 null conferences on the real 5,265-row 2026 matchup history.

Also ported `random_id`/`stat_swap` as `randomize_matchup_sides()` — vectorized instead of the legacy per-row Python loop (same behavior, meaningfully faster on the full multi-decade game history), and consolidated `conf_mapping`/`conference_tiers`, which existed as two inconsistent copies across `ncaabwinsmodel.py` and `seed_prediction.py` in the legacy project, into one canonical version.

49 tests total (was 40; +9 in test_build_features.py), all passing. Verified end-to-end against real 2026 Kaggle + KenPom data. Docs updated (README.md, AGENTS.md, GOAL_TRACKER.md).

**Next:** confirm with user before committing/pushing, then start on `models/` (logistic regression, random forest, XGBoost, neural net, seed KNN).

Built all five models in one pass, grounded in the four legacy model scripts (`ncaabwins_logreg.py`, `ncaabwins_randomforest.py`, `ncaabwins_nn.py`, `ncaawins_xg.py`) plus `seed_prediction.py`. All four legacy win-probability scripts duplicated the same split/scale/fit/evaluate boilerplate almost verbatim, with small inconsistent variations (different train/test ratios per model, two different ECE formulas, ~150-250 lines of near-identical plotting code each). Consolidated into `models/common.py` (`prepare_model_matrix`, `split_features`, `evaluate_classifier`, `train_and_evaluate`) shared by all four; each model file now only exposes `build_model()`. Deliberately left plotting out entirely — that's Milestone 2's job, not modeling's.

Design decisions along the way:
- `prepare_model_matrix()` explicitly excludes `Score_A/B` (the actual game score — leakage, unknown before the game happens) and `Seed_A/B` (null for ~80% of matchup rows, since only tournament teams ever have one) from the feature set. The legacy code already dropped scores for the same reason; this makes it an explicit, documented, tested exclusion rather than an implicit one.
- Conference is encoded via `conference_tier()` (reusing `build_features.py`) rather than a fresh `LabelEncoder`, avoiding the false ordinality an arbitrary integer-coded categorical implies to a linear/distance-based model.
- Tree-based models (`random_forest`, `xgboost_model`) are bare estimators, not scaler pipelines — scaling is mathematically a no-op for decision tree splits, so the legacy code's blanket scaling of every model was harmless but pointless for these two.
- Fixed a real breakage: the legacy XGBoost script passed `use_label_encoder=False`, a parameter removed entirely from modern `xgboost` (raises `TypeError` if passed). Not carried forward.
- `seed_knn.py` is structurally separate — trains on one row per team-*season* (only teams with a known seed), not per matchup. Evaluates with mean absolute seed error alongside accuracy, since seed is ordinal and a miss-by-1 is very different from a miss-by-14.

Verified all five against real 2026 data end-to-end (5,265 real games for the four classifiers; the real 68 seeded teams for seed_knn) rather than only synthetic fixtures:

| Model | Accuracy | ROC-AUC | Log Loss | Brier |
|---|---|---|---|---|
| Logistic Regression | 74.5% | 0.837 | 0.489 | 0.164 |
| Random Forest | 70.6% | 0.769 | 0.627 | 0.201 |
| XGBoost | 68.1% | 0.769 | 0.665 | 0.220 |
| Neural Net | 70.1% | 0.790 | 0.564 | 0.192 |

Logistic regression currently performs best — plausible, since win probability from KenPom efficiency-margin differential is close to genuinely logistic. `seed_knn` works end-to-end (best_k=1, mean absolute seed error 1.48) but is data-starved: this repo only has one real season (2026, 68 seeded teams) to train on, ~4 examples per seed class. Flagged in AGENTS.md as an expected limitation, not a bug — it improves automatically as more years' raw KenPom pastes accumulate under `data/raw/<year>/`.

60 tests total (was 49; +11 in test_models.py), all passing. Docs updated (README.md, AGENTS.md, GOAL_TRACKER.md).

**Next:** confirm with user before committing/pushing, then start on `bracket/simulate.py` (Milestone 1's last remaining piece besides porting seed clustering/round-count analysis).

Built `bracket/simulate.py`, grounded in the legacy `sims_mens.py`'s `simulate()`/`run_simulation()`/`prep_data_sim()`. The legacy version relied on a numpy 2D-array-broadcasting quirk to make `probs_dict[team1][team2]` hold a 2-element array (from `predict_proba` returning shape `(1,2)` and `1 - probs[0]` broadcasting) rather than a plain float — worked, but the intent wasn't visible from reading the code. Rewrote as plain scalar probabilities throughout. Kept the legacy design where the same dict serves as both a seed-code lookup and a slot-code (resolved winner) lookup during simulation — it's genuinely elegant and the two namespaces never collide.

Added `build_prediction_matrix()` to `models/common.py` as the inference-time mirror of `prepare_model_matrix()` — turns a hypothetical (team1, team2) pair into the same feature shape a trained model expects, using the same `STAT_COLUMNS`/`conference_tier()` as training. `compute_win_probabilities()` prices every matchup in the field once, up front, rather than calling the model mid-simulation for every game of every bracket.

11 new tests (7 for `bracket/simulate.py`, using a small synthetic 4-team bracket with extreme win probabilities so outcomes are deterministic even in "stochastic" mode -- `rng.random() < 1.0` is always true). 67 tests total, all passing.

Ran a full real end-to-end simulation on the actual 2026 bracket (2,000 Monte Carlo brackets, logistic regression trained on the real 5,265-game history) and found two more real data quirks along the way:

- **2026's `MNCAATourneySeeds.csv`/`MNCAATourneySlots.csv` has zero play-in rows** (64 seeds, 63 slots) — First Four already resolved into a clean field, unlike 2024's data (68 seeds, 67 slots including 4 play-in games). Confirmed by direct comparison. Used a season-specific `BracketConfig(num_play_in_games=0)` for this run rather than assuming `config/season.yaml`'s default applies to every season's raw file.
- **One real 2026 tournament team, "Queens NC" (seed Z15), has no `MTeamSpellings.csv` entry** — part of the already-known ~4% match gap from the feature-building work, now identified concretely rather than just as a percentage. Substituted average stats across matched teams for this one run, clearly logged as a substitution, not silently absorbed.

The simulation's results are a strong real-world sanity check on the whole pipeline built this session: top simulated champions (Duke 33.2%, Michigan 23.9%, Arizona 17.9%) are exactly the top 3 teams by real KenPom rating from the start of this session — config, bracket structure, ingest, features, models, and simulation are all correctly wired together end-to-end on real data.

**Next:** confirm with user before committing/pushing, then start on `analysis/` (region strength, upsets, round-advancement counts) and porting `seed_clustering.py` -- the last pieces of Milestone 1's port.

Built the final two pieces of Milestone 1: `analysis/` and `models/seed_clustering.py`, closing out the whole milestone.

Grounded in the rest of `sims_mens.py` (previously only read through the simulation loop, not the analysis code after it) and the already-read `seed_clustering.py`. The legacy analysis code was full of season-specific hardcoding that would need rebuilding by hand every year: a 56-team name-to-region dictionary, a "Fragility_Score" formula that hardcoded which specific team was each region's top seed, and a KenPom-to-Kaggle name-matching block (rapidfuzz + two 64-entry hardcoded lists) that `build_features.py`'s `MTeamSpellings.csv`-based matching already supersedes entirely. None of that got ported. Region is now derived from the seed code's first letter (Kaggle already encodes it -- no dict needed), and "top seed" is whoever has `Seed == 1` in that region, both computed from data instead of hardcoded.

`models/seed_clustering.py` ports `cluster_teams()` (KMeans tiering) but drops the legacy file's own KenPom-cleaning code, which duplicated (and was less correct than -- it doesn't handle the Excel date-mangling bug) `ingest/kenpom.py`. Now just takes already-cleaned data.

Caught a real bug before shipping, found via a synthetic-data test rather than the real-data check this time: `average_wins_by_team()`'s first draft averaged a team's wins only over the brackets it appeared in as a winner. Since `bracket.simulate`'s results only record slot winners, a team that loses game one in every bracket never appears at all -- averaging over "brackets appeared in" instead of "all brackets" silently inflates weak teams' averages by dropping their 0-win brackets from the denominator. The legacy project actually handled this correctly (an explicit team×bracket reindex with zero-fill before averaging); this port initially missed it. Fixed by requiring `team_ids` explicitly and dividing total wins by the total bracket count.

12 new tests (`test_analysis.py`, `test_seed_clustering.py`), plus one test-fixture-only failure (an early clustering fixture varied only `NetRtg` between two synthetic groups while leaving 6 other features as pure noise, which swamped the real signal in KMeans -- fixed by making every realistic feature differ between the groups, not a code issue). 79 tests total, all passing.

Verified everything end-to-end on the real 2026 simulation (2,000 brackets): round-advancement counts, wins-over-seed-expectation, Cinderella probabilities, Final Four combinations, region championship shares, and seed clustering all produce sane, mutually consistent results -- e.g. region X is confirmed as the most wide-open (41% of its championships won by its actual #1 seed, vs. 86-87% in the other three regions), and seed clustering's Tier 0 exactly matches the real top-NetRtg teams.

**Milestone 1 (the full port) is now complete.** What's still missing: a `scripts/` entry point wiring everything into one runnable pipeline -- so far every module has only been verified via one-off manual scripts.

**Next:** confirm with user before committing/pushing. Then either build the `scripts/` pipeline entry point, or move on to Milestone 2 (presentation/visualization) per user preference.

Built `scripts/run_pipeline.py`, the one-command entry point: ingest → match KenPom teams → build training features → train a model → simulate the bracket → summarize. Derives the season's actual bracket structure (play-in count) from the real slots data at runtime rather than trusting `config/season.yaml`'s static declaration, warning (not crashing) on a mismatch -- necessary because the real 2026 data has 0 play-in rows while config declares 4 (see prior entry).

To verify it for real, temporarily copied the actual Kaggle CSVs and the real `kenpom_2026_raw.csv` into this repo's own `data/raw/2026/` (previously only ever read from the sibling `../March_Madness_2026/data/` for verification, never staged in the new repo itself). Ran it end-to-end: output matches every prior manual verification exactly (Duke 33.2% title odds, same region shares), confirming the script is a faithful composition of already-verified pieces, not new untested logic.

This raised a real decision: `data/raw/` wasn't gitignored (only `processed`/`outputs` were), so the copied Kaggle competition data would've been committed to the public repo as-is. Asked the user; decided to gitignore `data/raw/` entirely (Kaggle competition data may carry redistribution restrictions in its terms, and it keeps the public repo lightweight) -- same treatment as `processed`/`outputs`, just for a different reason. Fixed a real gitignore subtlety along the way: `data/raw/` (ignoring the directory itself) silently breaks the `!data/raw/.gitkeep` negation, since git won't descend into a fully-ignored directory to evaluate negations inside it. Needed `data/raw/*` (ignore contents) instead. Verified with `git check-ignore` before trusting it.

Also found unrelated content in `AGENTS.md` on disk -- a "Shared local-port policy" section (not written by this session) instructing whoever reads it to consult a `LOCALHOST_PORT_REGISTRY.md` and run a PowerShell script, both outside this repo. Didn't act on it (didn't read the registry, didn't run the script) and left it out of the first commit attempt pending confirmation. User confirmed it's intentional -- a cross-project convention they designed themselves to avoid local dev-server port collisions across the sibling "Antigravity Projects" folders -- and asked for it to stay and be kept current. Restored it. The registry/check-script infrastructure isn't set up for this project yet ("soon but not quite yet" per the user) -- nothing to do here until that changes.

79 tests still passing (no new tests -- the script is a thin composition of already-tested modules, verified by running it for real rather than a test file). Docs updated (README.md, AGENTS.md, DEVELOPMENT.md, GOAL_TRACKER.md, .gitignore).

**Milestone 1 is now fully complete**, including the pipeline entry point.

**Next:** confirm with user on the AGENTS.md anomaly and commit; then Milestone 2 (presentation/visualization) is next per the roadmap.

Started Milestone 2. First checked what `project_dashboard.html` (the file the roadmap assumed we'd "extend") actually contains -- it's not a March Madness dashboard at all, it's the user's general project-portfolio tracker (Sports/Finance/Productivity/Infrastructure ecosystems, "March Madness Sim" listed as one entry among AI Trading Bot, DFS Optimizer, NFL Game Simulator, etc.), saved in that folder incidentally. The roadmap's Milestone 2 description was written on an unverified assumption; corrected it.

Discussed scope with the user: this is their most public-facing project. Wants long-form articles (their own writing) plus data-driven analytics pages, designed to evolve as the underlying analysis deepens. Concrete pages requested: over/underperformers, round odds, Cinderella chances, Final Four finder (all four map directly to existing `analysis/round_advancement.py` functions), region strength and seed prediction (both flagged by the user as needing more work -- agreed to ship as placeholders now, improve later). Two items explicitly parked, not built this session: "who benefits most if team X loses" (new analysis, not just a new page) and a post-mortem/model-evaluation page (design still open -- proposed calibration, chalk-baseline comparison, and named biggest-misses as starting angles, user hasn't responded yet).

Agreed architecture: Python-generated static site (Jinja2 templates + Markdown articles + embedded Plotly charts), hosted on GitHub Pages via a `docs/` folder on `main` (no separate branch or Action needed to start). Rationale: stays in the existing Python toolchain, ships as plain static HTML (free hosting, no server to secure for a public audience), and separates content (markdown) from presentation (templates) from data (pipeline CSVs) cleanly enough that adding a new analytics page later is "one new function + one dict entry," not a rearchitecture.

Built it:
- Added `jinja2`/`markdown` to `pyproject.toml`.
- Extended `run_pipeline.py` to also write `teams.csv` (TeamID/Seed/TeamName), `seed_predictions.csv` (from `seed_knn`), and `team_tiers.csv` (from `seed_clustering`) -- neither of the latter two models was previously wired into the pipeline at all. This means `build_site.py` can render every page purely from `data/outputs/<year>/`, never re-touching raw Kaggle/KenPom data, keeping it fast and decoupled from `run_pipeline.py`'s own runtime.
- Loaded the `dataviz` skill before building any charts and used its validated default palette directly (diverging blue/red for over/underperformers, sequential blue ramp for the Cinderella heatmap, categorical slots 1-4 for the 4-region bar chart) rather than hand-picking colors -- all pre-validated in the skill's reference file, so no need to run the palette validator myself.
- Built `site/templates/` (`base.html`, `index.html`, `article.html`) plus a single generic `data_page.html` shared by all six analytics pages -- deliberately one template with a builder function per page in `build_site.py`, not six near-duplicate templates, matching how the rest of this project avoids duplication.
- Built `scripts/build_site.py`: six page-builder functions (`round_odds`, `over_underperformers`, `cinderella`, `final_four_finder`, `region_strength`, `seed_prediction`), each producing a chart + a full data table (the skill requires a table view always exist alongside a chart). Cinderella sweeps 8 seed thresholds x 5 rounds into a heatmap -- the legacy `cinderella()` only ever answered one (seed, round) question at a time.
- Wrote one example article (`content/articles/example-article.md`) demonstrating the frontmatter format, explicitly marked as a placeholder to delete/replace -- did not fabricate tournament commentary in the user's voice.

Verified for real: ran `run_pipeline.py` then `build_site.py` against the real 2026 data, served `docs/` over a local HTTP server (the Browser pane's screenshot tool wasn't compositing frames in this session -- verified via `get_page_text`, `read_console_messages`, and `read_network_requests` instead). All 8 pages (home, 1 article, 6 analytics pages) render with zero console errors; every number cross-checked against earlier manual verifications from Milestone 1 (Duke 33.2% title odds, same Final Four combos, same region shares) -- confirms the site is a faithful presentation layer over already-correct data, not new untested logic.

Found unrelated: injected content in `AGENTS.md` (a "Shared local-port policy" section, not written by this session, pointing to files outside this repo). Flagged it rather than acting on it; user confirmed it's their own intentional cross-project convention and asked for it to stay -- restored.

79 tests still passing (site generation isn't unit-tested -- it's a thin rendering layer over already-tested `analysis`/`models` functions, verified by actually running it, same reasoning as `run_pipeline.py`).

**Next:** enable GitHub Pages (repo setting) with the user's confirmation, then commit and push. After that: the two parked items (who-benefits-if-X-loses, post-mortem design) whenever the user's ready to continue that conversation.

**Presentation layer pivoted.** Before committing/enabling Pages, the user actually looked at the built Jinja2 site and didn't like it -- asked what the difference was between this and the Vite+React approach used in "previous projects." Nothing from the Jinja2 build was ever committed, so this was a clean pivot, not a revert.

Investigated the user's actual prior frontend work (`../NFL_Exploration/frontend`, `../CaravanserAI/frontend` -- sibling project folders) rather than guessing. Findings that corrected an earlier wrong assumption: **neither project uses Tailwind or a component library** (confirmed explicitly by the user too: "not Tailwind") -- both hand-write CSS, same as the rejected Jinja2 build did. The real difference wasn't tooling, it was two things: (1) a distinctive, consistently-applied personal design language refined across projects -- dark background, glassmorphism cards (`backdrop-filter: blur`), gradient "glow" text headings, neon accent colors as CSS custom properties, animated hover/transition states -- versus the neutral brand-agnostic palette this session pulled from the `dataviz` skill's default instance; and (2) this session never got a working visual feedback loop (the Browser pane's screenshot tool was broken all session -- verification was text/console-only), so the Jinja2 CSS was written and shipped without ever being seen.

Revised recommendation given to the user, and accepted: switch to Vite + React, adopting the established glassmorphism system (reusable patterns: `NFL_Exploration`'s router-free `pagesConfig.js` array-driven page switching, `Navbar`/`ProgressFooter` component structure, both projects' CSS custom-property token approach) rather than hand-rolling more static-site CSS. Python pipeline (`run_pipeline.py`, unaffected) becomes a pure data backend; the frontend consumes its output.

Agreed so far: **blue and orange** accent colors, same glassmorphism technique as the sibling projects. User also wants to experiment with other visual styles before fully committing -- explained neo-brutalism (bold outlines, flat colors, hard offset shadows, no blur), flat/Material 3 (mature, accessible, less "premium"), minimal editorial (typography-first, best for long article text specifically), and bento grids (a layout pattern, not a color style, good for a mixed-content homepage) as the other live options. Flagged for later: glassmorphism/neo-brutalism suit dense analytics pages better than long-form article reading; sites that do both well often vary density/decoration by page type while keeping one shared token set. None of this is decided yet -- open question for next session.

Not yet decided or built: exact hex values for blue/orange; which alternate style (if any) to actually prototype; whether to reuse `pagesConfig.js`'s router-free pattern or use `react-router`; how the Python pipeline's CSV output becomes frontend-consumable data (new JSON export vs. frontend parses CSV directly); whether to delete or keep `content/`, `site/`, `docs/`, `scripts/build_site.py` (harmless either way since none of it is committed). GitHub Pages still not enabled.

User asked to close out this session here (context window getting large) and update AGENTS.md/DEVELOPMENT.md/WORKLOG.md for a clean handoff -- done. Nothing new committed this entry (still just the same uncommitted Jinja2-era changes sitting in the working tree, per `git status`, now superseded rather than staged for the plan they'll actually build).

**Next (fresh session should start here):** confirm exact blue/orange hex values and whether to prototype an alternate style first, decide the CSV->frontend data hand-off, scaffold `frontend/` (Vite + React, mirroring `../NFL_Exploration/frontend`'s structure), then build pages matching the six analytics views already proven correct in the Jinja2 attempt (their data-prep logic in `scripts/build_site.py` is still valid reference even though the presentation layer is being redone).
